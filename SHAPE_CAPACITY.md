# SHAPE_CAPACITY — 만들 수 있는 공학 형상 집계

> 2026-07-08 기준. 5개 병렬 코드 리더가 실제 소스를 전수 조사해 집계
> (원시 155종 shape_types / 285개 이산 변형 → 중복 제거).

## 🆕 2026-07-10 확장 (집계 이후 추가된 형상 패밀리)

| 패밀리 | 내용 | 검증 |
|---|---|---|
| **IC-패키지 스택** | 볼맵 파일 → 솔더볼(실린더/배럴) + 다이 + 수지 매트릭스 + 레이어 적층 (`generate_package_from_file`) | g20 5/5 |
| **체결(Fastener)** | 동심원 홀쌍 감지 → 볼트/리벳 × 헤드 6종(hex/socket/pan/csk/dome/flat) × 나사산 3종(rings/simplified/none) + 너트/와셔, ISO 262/724 파라메트릭 (`suggest/add_fastener`) | g21 6/6 + g22 8/8 |
| **일반 CAD 프리미티브** | **회전체**(임의축/부분각)·**스윕**(아크 코너 파이프)·**로프트**(원/사각/다각 섹션)·평면 **분할**·**구배**·이동/회전/스케일/**패턴** (`revolve/sweep/loft/split/draft/transform`) | g23 10/10 |

MCP 도구 **56개** (라이브 DLL 스키마 전수 JSON-유효 검증, 2026-07-10). 연속 곡면
패밀리(회전체·스윕·로프트) 추가로 축대칭 부품·배관·천이형상이 자연어로 생성 가능.
⚠️ Helix는 API에 public 팩토리가 없어 진짜 나선(스프링/실나사)은 불가 — 링 근사가 상한.

## 한 줄 답

**고유 형상/피처 생성 능력 ≈ 88~90종, 이름 붙은 표준·변형 프리셋 ≈ 130+개.**
각 형상이 연속 치수 파라미터를 갖고 한 몸체에 자유롭게 중첩되므로
**실제 설계 공간은 사실상 무한(unbounded)**.

## 분류별 집계

| 카테고리 | 형상/피처 종류 | 이산 프리셋 | 대표 예시 |
|---|---|---|---|
| **폰-메탈 파이프라인 (S00–S12)** | 21 | ~30 | 슬래브, 곡면 백벌지, 카메라(원통/라운드렉트), 렌즈홀, 포트슬롯, 그릴, 버튼, 안테나 슬릿, 챔퍼(4필터) |
| **시험시편 표준** | 30 | **58** | 도그본, V/U노치, OHT/OHC, Iosipescu 전단, 3·4점 굽힘, CT/MT 균열, 랩전단 조인트 |
| **MCP 형상 도구** | 44 (신규생성 ~19) | cut_void 9, 패턴 3, 챔퍼 4 | add_hole/boss/slit/pocket/rib, *_on_face(곡면), cut_void, generate_phone_from_spec |
| **라미네이트·픽스처·프리미티브** | 43 | ~40 | N-플라이 적층(X/Y/Z), void 커터(육면체/원통/구), 굽힘 픽스처, 프로파일(선/호/원/사각) |
| **역설계 인식(왕복)** | 18 | 패턴 3×2 | 홀(원통/원뿔), 보스(원통/테이퍼), 벽, 필렛, 슬릿, 포켓, 리브, 대칭 |
| **원시 합계(중복 포함)** | ~155 | ~285 | — |
| **✅ 중복 제거 후 순 능력** | **≈ 88~90** | **≈ 130+** | — |

### 중복 제거 근거

MCP의 add_hole/boss/slit/pocket/rib/chamfer/*_on_face/cut_void 는 실제로
`ModificationService`의 동일 커널 프리미티브로 디스패치되며, 폰 파이프라인(S00–S12)·
RE 인식 피처와 물리적으로 같은 형상이다. 인장 26·CAI 3·피로 6·조인트 5는
시편 패밀리와 프리미티브 패밀리 양쪽 census에 중복 보고 → 걷어내면 ~88~90종.

## 왜 사실상 "무한"인가

각 형상은 N개의 연속 치수를 갖는다(슬롯 3개, 라운드렉트 카메라 5개,
`generate_phone_from_spec` 20개). 폰 파이프라인은 한 몸체에 13단계를 순차 중첩하고,
`apply_operations`가 44개 도구를 임의 순서·개수로 조합하므로 설계는 **연속 × 조합**으로 폭발.

**워크드 추정(이산만):** 카메라 2 × 홀 경로(평/곡) 2 × 포트 배치 2 × 안테나 2 ×
그릴 2 × 챔퍼 4 × 필렛 4 × 홀패턴 3 = **3,072가지 이산 골격** — 여기에 모든 치수가
연속이라 실질 무한대.

## 가장 인상적인 숫자 3개

- **26개 표준 인장시편** — `generate_tensile_specimen` 하나가 ASTM E8·D638 Type I–V·ISO 527·DMA 등 국제 규격을 enum 하나로 즉시 생성
- **13단계(S00–S12)에 21종 형상 피처** — 자연어 → SpecParser → JSON spec 하나가 곡면 백·카메라·포트·그릴·안테나까지 전 파이프라인 구동 (g9 capstone 실증)
- **cut_void 하나 = 9조합** (형상 3종 × 불리언 3모드), `apply_operations`로 무한 배치 합성

## 주요 근거 소스

- `Services/ReverseEngineer/Generation/GenerationService.cs` — S00–S12 파이프라인
- `Models/TensileTest/ASTMSpecimenType.cs` — 26 인장 표준
- `Services/ReverseEngineer/LlmToolRegistry.cs` — 44 MCP 도구
- `Services/ReverseEngineer/ModificationService.cs` — 공통 형상 프리미티브 디스패치
